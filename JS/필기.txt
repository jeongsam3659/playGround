외부 스크립트 파일 연결하기.

이 작업을 하는 이유.
 > 큰 규모의 프로젝트 진행시 JS소스를 분리하는것이 좀더 효율적임을 알 수 있다.
 > CSS 하나를 만들고 각각html에 적용된 것과 같은 말.


 JS규칙
  1. 대소문자를 구별하여 소스 작성
    sum / Sum / SUM
  
  2. 입게 쉽게 들여 쓰는 습관

  3. 세미콜론.

  4. 메모시 주석. //

예시1)
  let today = new Date(); //날짜를 가져온다.

  5. 식별자는 정해진 규칙을 지켜서 작성 및 예약어 사용x
    변수/함수/속성 등을 구별하기 위해 특정단어를 붙는데

    영문자로 식별자 = num1
    _doSomething = (_)로 시작하는 식별자
    checkTime() = 두 단어로 만든 식별자

    

----------------------------------------------------------------
 p70
    undefined / null

  JS는 변수는 선언할때 미리 자료형을 지정하지않고 값을 할당할 때 그 값에 따라 결정.
  
  undefined은 자료형이 정의되지 않았을때 상태. '처음부터 변수에 값이 할당되지 않았다'
  null은 '처음에 할당된 값이 더는 유효하지않음'

p74
    Js와 비슷한 TS타입스크립트
  
  Js는 웹문서에 동적을 추가하기위한 가벼운 언어. 자료형을 고려하여 만들지 않았다.
  자료형을 강하게 확인하는게 ' 타입스크립트 '


p83
    ES6 템플릿 문자열

  `${ }`

  `백쿼드 / ${ 변수명 } 

p89
    1. 나이계산에 사용할 변수 선언. brith year age
    2. 올해 연도 값 year
    3. prompt()함수로 태어난 연도 입력받아 변수에 할당
    4. 변수로 나이 계산 나이= 현재 - 태어난연도 +1
    5. alert() 함수로 결과값 출력

    1. 원래 가격 텍스트 필드에서 값 가져와 변수에 저장. originPrice
    2. 할인율 가져와 변수에 저장. rate
    3. 원래 가격과 할인율 값이 있을때만 실행. &&연산자
       할인된 가격 계산해 변수에 저장 savedPrice
       최종 가격 (원가 - 할인가) 계산해서 저장 resultPrice
       #showResult 영역에 결과값 표시

p116
    ! 팩토리얼 계산
    1!  1
    2!  2*1
    3!  3*2*1
    ...

    var nFact = 1; // 1을 기본값으로 지정
    var i = 2; // 1! = 1이기때문에